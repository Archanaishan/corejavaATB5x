package src.jan19.abstraction;

public class readme {
    //showing the essential informations to the user and hiding the rest inner details
    // to reduce complexity is called abstraction
    //encapsulation-is within the class
    //abstraction-if there are multiple classes and we want only few classes
    //abstraction can be done through abstract class and interface
    //abstract also means partial implementation.those partial methods will be implemented in child classes
    //all child classes extending abstract class should implement the partial classes
}
